---
name: run-mcp-usability-pass
description: Exercise Focus the way an agent uses it: bootstrap, discovery, retrieval, governance, and duplicate-safe save flows.
category: Task
pinned: true
trigger_hints: mcp, usability, context.inspect, bootstrap, governance, duplicates
---

# Run an MCP usability pass

Exercise Focus the way an agent uses it: bootstrap, discovery, retrieval, governance, and duplicate-safe save flows.

## When to Use This Skill

Use this after MCP changes, retrieval tuning, or when Focus feels awkward and needs a practical end-to-end usability check.

## Workflow Overview

1. Initialize an MCP session and verify the manifest loads.
2. Start with workspace bootstrap and wing or room discovery for the area you care about.
3. Run a realistic context question and inspect whether durable memories lead the results.
4. Test governance and duplicate-safe save flows with dry-run mode.
5. Record any friction as a concrete follow-up item instead of a vague usability complaint.

## Examples

- Do a live MCP usability pass for Microsoft-related memories.
- Check whether duplicate-safe save catches an Entra mailbox draft.
- Verify context.inspect leads with durable memory instead of code graph noise.
